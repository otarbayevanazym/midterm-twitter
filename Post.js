// src/components/Post.js
import React, { useState } from 'react';
import './Post.css';

function Post({ post, posts, setPosts }) {
  // ... (Use the code provided earlier for Post.js)

  return (
    <div className="Post">
      {/* ... (Use the code provided earlier for Post.js) */}
    </div>
  );
}

export default Post;
